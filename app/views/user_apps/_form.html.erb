<%= form_for(@user_app) do |f| %>
  <% if @user_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_app.errors.count, "error") %> prohibited this user_app from being saved:</h2>

      <ul>
      <% @user_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_area :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
