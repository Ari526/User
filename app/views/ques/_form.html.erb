<%= form_for(@que) do |f| %>
  <% if @que.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@que.errors.count, "error") %> prohibited this que from being saved:</h2>

      <ul>
      <% @que.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
