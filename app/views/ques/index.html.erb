<p id="notice"><%= notice %></p>

<h1>Listing Ques</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ques.each do |que| %>
      <tr>
        <td><%= que.title %></td>
        <td><%= link_to 'Show', que %></td>
        <td><%= link_to 'Edit', edit_que_path(que) %></td>
        <td><%= link_to 'Destroy', que, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Que', new_que_path %>
